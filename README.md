# Shop-
Shop Test
git init
git branch -M main
git add .
git commit -m "Initial commit"
git remote add origin YOUR_REPO_URL
git push -u origin main
git add .
git commit -m "Initial commit"
git push -u origin main
